const express=require('express');const nunjucks=require('nunjucks');const path=require('path');const routeStatic=require('./lib/route-static');const redirectIndices=require('./lib/redirect-indices');const app=express();const baseDir='src';const port=process.env.PORT||3004;app.set('etag',!1);app.use((req,res,next)=>{res.removeHeader('X-Powered-By');next()});app.use(routeStatic);app.use('/static',express.static(path.join(__dirname,baseDir),{etag:!1,lastModified:!1}));app.use(redirectIndices);nunjucks.configure(baseDir,{autoescape:!0,express:app,watch:!0});app.get('*',(req,res,next)=>{res.render(path.join('./',req.url,'index.html'),{})});app.listen(port,(err)=>{err?console.error(err):console.log(`app running on http://localhost:${port}`)})
